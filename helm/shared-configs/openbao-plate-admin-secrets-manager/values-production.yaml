# Production environment specific values for plate-admin secrets

global:
  namespaces:
    - plate-prod

environments:
  production:
    namespace: "plate-prod"
    remoteRef:
      key: "plate-admin/production"

externalSecrets:
  secretStore:
    name: "openbao-plate-admin-env-production"
    labels:
      environment: production
      tier: production
    annotations:
      description: "OpenBao SecretStore for plate-admin production environment"

  externalSecret:
    name: "plate-admin-env-secrets-production"
    refreshInterval: "1h"
    target:
      name: "plate-admin-env-secrets"
      template:
        metadata:
          labels:
            environment: production
            tier: production
            backup: "enabled"
