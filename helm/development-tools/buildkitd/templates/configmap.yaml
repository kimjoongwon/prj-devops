apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "buildkitd.fullname" . }}-config
  labels:
    {{- include "buildkitd.labels" . | nindent 4 }}
data:
  buildkitd.toml: |
    {{- .Values.config | nindent 4 }}
