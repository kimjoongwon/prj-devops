apiVersion: v1
kind: Service
metadata:
  name: {{ include "be-llm.fullname" . }}
  labels:
    {{- include "be-llm.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "be-llm.selectorLabels" . | nindent 4 }}
