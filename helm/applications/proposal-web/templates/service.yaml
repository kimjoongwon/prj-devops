# Proposal Web Service
apiVersion: v1
kind: Service
metadata:
  name: {{ include "proposal-web.name" . }}
  labels:
    {{- include "proposal-web.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "proposal-web.selectorLabels" . | nindent 4 }}
