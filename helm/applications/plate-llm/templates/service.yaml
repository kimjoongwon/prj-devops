apiVersion: v1
kind: Service
metadata:
  name: {{ include "plate-llm.name" . }}
  labels:
    {{- include "plate-llm.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "plate-llm.selectorLabels" . | nindent 4 }}
