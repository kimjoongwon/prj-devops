apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "idp-api.name" . }}
  labels:
    {{- include "idp-api.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      {{- include "idp-api.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "idp-api.labels" . | nindent 8 }}
      annotations: {}
    spec:
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- if and .Values.otel .Values.otel.enabled }}
      # OTel nodejs 자동계측 — 모듈을 emptyDir로 복사 (NODE_OPTIONS로 로드)
      initContainers:
        - name: otel-autoinstrumentation
          image: "otel/autoinstrumentation-nodejs:{{ .Values.otel.imageTag }}"
          command: ["sh", "-c", "cp -r /autoinstrumentation/. /otel-ai/"]
          volumeMounts:
            {{- if and .Values.otel .Values.otel.enabled }}
            - name: otel-ai
              mountPath: /otel-ai
            {{- end }}
      {{- end }}
      containers:
        - name: {{ include "idp-api.name" . }}
          image: "{{ index .Values "idp-api" "image" "repository" }}:{{ index .Values "idp-api" "image" "tag" | default .Chart.AppVersion }}"
          imagePullPolicy: {{ index .Values "idp-api" "image" "pullPolicy" }}
          {{- with .Values.command }}
          command:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          {{- with .Values.args }}
          args:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          ports:
            - name: http
              containerPort: {{ index .Values "idp-api" "port" }}
              protocol: TCP
          {{- with index .Values "idp-api" "probes" "startup" }}
          startupProbe:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          {{- with index .Values "idp-api" "probes" "readiness" }}
          readinessProbe:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          {{- with index .Values "idp-api" "probes" "liveness" }}
          livenessProbe:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          {{- with .Values.extraEnv }}
          env:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          {{- $hasAppSecrets := .Values.appSecrets.enabled }}
          {{- $hasInfraSecrets := and .Values.infraSecrets .Values.infraSecrets.enabled }}
          {{- if or $hasAppSecrets $hasInfraSecrets }}
          envFrom:
            {{- if $hasAppSecrets }}
            - secretRef:
                name: {{ .Values.appSecrets.secretName }}
            {{- end }}
            {{- if $hasInfraSecrets }}
            - secretRef:
                name: {{ .Values.infraSecrets.secretName }}
            {{- end }}
          {{- end }}
          {{- if and .Values.otel .Values.otel.enabled }}
          volumeMounts:
            - name: otel-ai
              mountPath: /otel-ai
          {{- end }}
          resources:
            {{- toYaml (index .Values "idp-api" "resources") | nindent 12 }}

      {{- if and .Values.otel .Values.otel.enabled }}
      volumes:
        - name: otel-ai
          emptyDir: {}
      {{- end }}
