apiVersion: v1
kind: Service
metadata:
  name: {{ include "idp-api.name" . }}
  labels:
    {{- include "idp-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "idp-api.selectorLabels" . | nindent 4 }}
